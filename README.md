Usage:
1. Download the project and install dependencies (uv recommended: `uv sync`)
1. Provide a `GOOGLE_API_KEY` or `AZURE_API_KEY` in `.env` (or point `CUSTOM_API` to a local server) 
3. Run the app with `streamlit run src/app.py` and have fun!
